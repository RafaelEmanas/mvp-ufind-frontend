import { Component } from '@angular/core';
import { LoginBackButton } from '../login-back-button/login-back-button';

@Component({
  selector: 'app-login-header',
  imports: [LoginBackButton],
  templateUrl: './login-header.html'
})
export class LoginHeader {

}
